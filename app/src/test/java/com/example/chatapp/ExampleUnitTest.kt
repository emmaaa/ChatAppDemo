package com.example.chatapp

import org.junit.Test
import org.junit.Assert.*

/**
 * Placeholder unit test. See dedicated test files in subdirectories.
 */
class ExampleUnitTest {
    @Test
    fun placeholder() {
        // All tests are in domain/, data/, and presentation/ packages
        assertTrue(true)
    }
}